<style>
    .itemImagesBox {
        /* Display */
        display: block;

        /* Size */
        width: 100%;
        height: fit-content;
    }

    .itemImagesBox .imageDisplayBox {
        /* Display */
        display: block;

        /* Size */
        width: fit-content;
        height: fit-content;

        /* Margin */
        margin-bottom: 32px;
    }

    .imageDisplayBox img {
        /* Size */
        width: 250px;
        height: 250px;
    }

    .itemImagesBox .imageSelectionBox {
        /* Display */
        display: block;

        /* Size */
        width: inherit;
        height: fit-content;

        /* Text align */
        text-align: left;

        /* White space */
        white-space: nowrap;

        /* Overflow */
        overflow: scroll;
    }

    .imageSelectionBox .imageSelectionCell {
        /* Display */
        display: inline-block;

        /* Size */
        width: 50px;
        height: 50px;

        /* Padding */
        padding: 5px;

        /* Cusor */
        cursor: pointer;
    }

    .imageSelectionCell img {
        /* Size */
        width: inherit;
        height: inherit;
    }
</style>